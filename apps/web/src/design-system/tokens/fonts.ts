export const cabFonts = {
  display: "$heading",
  heading: "$heading",
  body: "$body",
  ui: "$body",
  data: "$mono",
  mono: "$mono",
  brand: "$heading",
  dataAccent: "$mono",
} as const;

export const cabFontWeights = {
  regular: 400,
  medium: 500,
  semibold: 600,
  bold: 700,
} as const;
